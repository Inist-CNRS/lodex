import expect from 'expect';

import { uploadMiddleware } from './upload';

describe('upload', () => {
    it('should return set status to 500 and body to error message if parsing throw an error', async () => {

    });
});
