import React from 'react';
import translate from 'redux-polyglot/translate';

export const defaultArgs = {};

const LdaChartAdmin = () => {
    return <div>{'Nothing to show for now'}</div>;
};

export default translate(LdaChartAdmin);
