# Login et mot de passe pour créer et administrer des instances (par défaut : root/secret)
# ROOT_LOGIN=
# ROOT_PASSWORD=

# Nom et port du serveur ou MongoDB est installé
# MONGO_HOST=localhost:27017

# Derrière un reverse proxy, il peut être nécessaire de donner l'url du réeseau interne
# Celle-ci doit être joinable par les web services de précalcul
# Si elle n'est pas renseignée, l'url sera calculée automatiqument.
# PRECOMPUTED_URL=http://locahost:3000
